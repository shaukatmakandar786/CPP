#include <iostream>

using namespace std;

int main()
{
    int *p=NULL;
    float *p1=NULL;
    char *c=NULL;
    
    cout<<sizeof(p);
    cout<<sizeof(p1);
    cout<<sizeof(c);

    return 0;
}

// 888
